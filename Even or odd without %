#include<iostream>
#include<bits/stdc++.h>
using namespace std;

bool check(int n)
{
  return ((n/2)*2 == n);
}

int main()
{
  int n;
  cout<<"Enter the number: ";
  cin>>n;
  cout<<n<<" is ";

  check(n) ? cout<<"Even" : cout<<"Odd";
  return 0;

}

      OUTPUT
Enter the number: 44
44 is Even
